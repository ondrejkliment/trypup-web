// @ts-check
import { defineConfig } from 'astro/config';

// Statický web (default output) — Vercel ho auto-detekuje, žádný adapter netřeba.
export default defineConfig({
  site: 'https://trypup.life',
});
